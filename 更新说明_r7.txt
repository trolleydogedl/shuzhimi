数之谜 GitHub Pages 网站公式显示与居中排版修复版 r7

本次重点修复：
1. 修复公式中的 < 和 > 被浏览器误识别为 HTML 标签，导致 q<n 显示成 qn、后续中文和公式被吞掉的问题。
2. 显示公式恢复居中，不再整页靠左。
3. 连续的短公式按可用宽度居中并排，空间不足时自然换行。
4. 长公式优先在保证可读性的前提下缩放；只有缩放后仍放不下时才启用局部横向滚动。
5. 保留 \left / \right 安全修复，并新增对少数缺失单个 $ 结束符的保守恢复。
6. 题库原数据无需重新导入，覆盖部署网站文件后即可修复现有题目的展示。
